<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/menu_home"
            android:action="@+id/nav_home"/>

        <item
            android:id="@+id/bookListFragment"
            android:icon="@drawable/gavel"
            android:title="@string/menu_book"
            android:action="@+id/bookListFragment"/>
        <item
            android:id="@+id/bukuListFragment"
            android:icon="@drawable/diversity"
            android:title="@string/menu_buku"
            android:action="@+id/bukuListFragment"/>
    </group>
</menu>